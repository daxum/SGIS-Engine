This folder contains some default shaders to be used with the engine. The engine will load all the shaders it needs from
here if this folder is specified with the EngineConfig.shaderPath variable during engine instantiation.
